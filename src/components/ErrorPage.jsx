import { Button, Card } from "react-bootstrap";

const ErrorPage = () => {
  return (
    <Card className="w-100 text-center p-4 text-primary">
      Something went wrong
    </Card>
  );
};

export default ErrorPage;
